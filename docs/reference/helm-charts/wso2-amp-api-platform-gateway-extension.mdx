---
title: API Platform Gateway Extension
---

# API Platform Gateway Extension

A Helm chart to deploy and auto-register the unified API Platform Gateway for the Agent Management Platform.

Helm chart values reference for `wso2-amp-api-platform-gateway-extension`.

```bash
helm install amp-api-platform-gateway-extension oci://ghcr.io/wso2/helm-charts/wso2-amp-api-platform-gateway-extension \
  --namespace <namespace> --create-namespace \
  --values my-values.yaml
```

## agentManager

| Parameter | Description | Type | Default |
|---|---|---|---|
| `agentManager.apiUrl` | Public API URL (port 8080 = JWT-authenticated API) | string | `"http://amp-api.wso2-amp.svc.cluster.local:9000/api/v1"` |
| `agentManager.orgName` | Organization name in Agent Manager (must match Thunder OU handle) | string | `"default"` |
| `agentManager.idp.tokenUrl` |  | string | `"http://amp-thunder-extension-service.amp-thunder.svc.cluster.local:8090/oauth2/token"` |
| `agentManager.idp.clientId` |  | string | `"amp-api-client"` |
| `agentManager.idp.clientSecret` |  | string | `"amp-api-client-secret"` |
| `agentManager.idp.existingSecret` | Reference an existing secret for IDP credentials (recommended for production). When set, clientId and clientSecret above are ignored. | string | `""` |
| `agentManager.idp.existingSecretClientIdKey` |  | string | `"client-id"` |
| `agentManager.idp.existingSecretClientSecretKey` |  | string | `"client-secret"` |

## apiGateway

| Parameter | Description | Type | Default |
|---|---|---|---|
| `apiGateway.namespace` | Namespace where the APIGateway CR is created. Must be the same namespace where the gateway-operator is watching. For per-org-env namespace isolation set this to "&lt;org&gt;-&lt;env&gt;" (add-environment.sh passes it via --set apiGateway.namespace=...). | string | `"openchoreo-data-plane"` |
| `apiGateway.controlPlane.host` |  | string | `"amp-api-gateway-manager.wso2-amp.svc.cluster.local:9243"` |
| `apiGateway.controlPlane.tls.enabled` |  | boolean | `true` |
| `apiGateway.config.configMapName` | Name of the ConfigMap to create with gateway Helm values. Defaults to "&lt;release-name&gt;-config" if empty. | string | `""` |
| `apiGateway.config.chartVersion` | Helm chart version for the gateway (must match what gateway-operator is configured for) | string | `"1.1.0"` |
| `apiGateway.config.policyConfigurations.ratelimit_v1.algorithm` |  | string | `"fixed-window"` |
| `apiGateway.config.policyConfigurations.ratelimit_v1.backend` |  | string | `"memory"` |
| `apiGateway.config.policyConfigurations.ratelimit_v1.memory.max_entries` |  | integer | `10000` |
| `apiGateway.config.policyConfigurations.ratelimit_v1.memory.cleanup_interval` |  | string | `"5m"` |
| `apiGateway.config.policyConfigurations.llm_cost_ratelimit_v1.cost_scale_factor` |  | integer | `1000000000` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.keymanagers` |  | list | see `values.yaml` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.issuer` |  | string | `agent-manager-service` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.jwks.remote.uri` |  | string | `http://amp-api.wso2-amp.svc.cluster.local:9000/auth/external/jwks.json` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.jwks.remote.skipTlsVerify` |  | boolean | `true` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.issuer` |  | string | `http://thunder.amp.localhost:8080` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.jwks.remote.uri` |  | string | `http://amp-thunder-extension-service.amp-thunder:8090/oauth2/jwks` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.jwks.remote.skipTlsVerify` |  | boolean | `true` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.authheaderscheme` |  | string | `""` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.onfailurestatuscode` |  | integer | `401` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.errormessageformat` |  | string | `json` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.errormessage` |  | string | `"Authentication failed"` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.leeway` |  | string | `30s` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.allowedalgorithms` |  | list | see `values.yaml` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.jwkscachettl` |  | string | `5m` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.jwksfetchtimeout` |  | string | `5s` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.jwksfetchretrycount` |  | integer | `3` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.jwksfetchretryinterval` |  | string | `2s` |
| `apiGateway.config.policyConfigurations.jwtauth_v1.validateissuer` |  | boolean | `true` |
| `apiGateway.config.systemEnv` | Root-level config keys injected as environment variables into the policy engine. Use for policies that reference $&#123;config.&lt;key&gt;&#125; at root level (e.g., AWS Bedrock, Azure Content Safety, embedding providers). Keys are converted to env vars: key_name -&gt; APIP_GW_KEY__NAME (underscores become double underscores to preserve them in koanf). Example: systemEnv: # AWS Bedrock Guardrail awsbedrock_guardrail_region: "us-east-1" awsbedrock_guardrail_id: "" awsbedrock_guardrail_version: "DRAFT" awsbedrock_access_key_id: "" awsbedrock_secret_access_key: "" awsbedrock_session_token: "" awsbedrock_role_arn: "" awsbedrock_role_region: "" awsbedrock_role_external_id: "" # Azure Content Safety azurecontentsafety_endpoint: "" azurecontentsafety_key: "" # Embedding Provider (semantic-cache, semantic-prompt-guard, semantic-tool-filtering) embedding_provider: "OPENAI" embedding_provider_endpoint: "" embedding_provider_model: "text-embedding-ada-002" embedding_provider_dimension: "1536" embedding_provider_api_key: "" # Vector DB (semantic-cache) vector_db_provider: "REDIS" vector_db_provider_host: "" vector_db_provider_port: "6379" vector_db_provider_username: "" vector_db_provider_password: "" vector_db_provider_database: "" vector_db_provider_ttl: "3600" | object | `{}` |

## bootstrap

| Parameter | Description | Type | Default |
|---|---|---|---|
| `bootstrap.enabled` |  | boolean | `true` |
| `bootstrap.backoffLimit` |  | integer | `3` |
| `bootstrap.identityProviders` | Identity providers to mirror into Agent Manager (AMS) for this gateway. This list is the authoritative allowlist of IdPs the bootstrap job seeds via the AMS identity-provider API. It is intentionally decoupled from apiGateway.config.policyConfigurations.jwtauth_v1.keymanagers: that list is the gateway runtime's JWT-validation config (and legitimately includes the internal 'agent-manager-service' keymanager and ThunderKeyManager), whereas this list is what AMS exposes in the Security UI and as agent OAuth issuer options. Only list user-visible providers here — omit any internal/reserved keymanagers (e.g. agent-manager-service). Defaults to empty: no IdP is auto-mirrored into AMS. ThunderKeyManager stays a gateway keymanager (so deployed agents still validate Thunder-issued tokens), but is no longer registered as a visible identity provider by default — operators add IdPs on demand via the Manage Identity Provider flow. The bootstrap job seeds exactly the entries listed here, so an empty list seeds nothing. Each entry: &#123; name, issuer, jwksUri, skipTlsVerify &#125;. | list | `[]` |
| `bootstrap.ttlSecondsAfterFinished` | TTL after completion (seconds). 86400 = 1 day | integer | `86400` |
| `bootstrap.resources.requests.memory` |  | string | `64Mi` |
| `bootstrap.resources.requests.cpu` |  | string | `50m` |
| `bootstrap.resources.limits.memory` |  | string | `128Mi` |
| `bootstrap.resources.limits.cpu` |  | string | `100m` |
| `bootstrap.podSecurityContext.runAsNonRoot` |  | boolean | `false` |
| `bootstrap.podSecurityContext.runAsUser` |  | integer | `0` |
| `bootstrap.podSecurityContext.fsGroup` |  | integer | `0` |
| `bootstrap.securityContext.runAsNonRoot` |  | boolean | `false` |
| `bootstrap.securityContext.runAsUser` |  | integer | `0` |
| `bootstrap.securityContext.allowPrivilegeEscalation` |  | boolean | `false` |
| `bootstrap.securityContext.capabilities.drop` |  | list | see `values.yaml` |
| `bootstrap.securityContext.readOnlyRootFilesystem` |  | boolean | `false` |

## developmentMode

| Parameter | Description | Type | Default |
|---|---|---|---|
| `developmentMode` | APIGateway CR configuration (deployed by gateway-operator) Development mode | boolean | `true` |

## gateway

| Parameter | Description | Type | Default |
|---|---|---|---|
| `gateway.name` | Name of the AI gateway to register in Agent Manager (lowercase alphanumeric + hyphens). Defaults to the Helm release name if empty. | string | `""` |
| `gateway.displayName` | Human-readable display name | string | `"Default API Gateway"` |
| `gateway.vhost` | Virtual host (FQDN or IP) that the gateway is reachable on. All access is hostname-routed through kgateway — never the runtime Service directly. | string | `"http://default-default.gateway.localhost:19080"` |
| `gateway.runtimeUrl` | In-cluster base URL sandboxed agents use to reach this gateway runtime. Empty derives "http://&lt;apiGatewayName&gt;-gw-gateway-gateway-runtime.&lt;apiGateway.namespace&gt;:22893". AMS validates it: http/https, explicit port that is not 80 or 443, cluster-local host. | string | `""` |
| `gateway.hostname` | Hostname for routing traffic to this gateway through kgateway. All traffic (LLM proxy, OTEL, etc.) matching this hostname on kgateway is forwarded to this gateway's runtime service. Defaults to "&lt;environment&gt;-&lt;orgName&gt;.gateway.localhost" if empty. | string | `""` |
| `gateway.type` | Gateway placement role: INGRESS, EGRESS, or BOTH (must match GatewayType enum in Agent Manager API). BOTH is correct for single topology, where the chart is installed once per environment; split topology sets this explicitly on both releases from add-environment.sh. Immutable after first registration — changing it later only logs a drift warning. | string | `"BOTH"` |
| `gateway.environment` | Environment name in Agent Manager to associate this gateway with. The bootstrap job looks up environments by this name. | string | `"default"` |
| `gateway.tokenSecret.name` |  | string | `""` |
| `gateway.tokenSecret.key` |  | string | `"token"` |

## kgateway

| Parameter | Description | Type | Default |
|---|---|---|---|
| `kgateway.name` |  | string | `"gateway-default"` |
| `kgateway.namespace` |  | string | `"openchoreo-data-plane"` |

## kubectl

| Parameter | Description | Type | Default |
|---|---|---|---|
| `kubectl.image` |  | string | `alpine/k8s:1.32.3` |

